---
interface Props {
  className?: string
  href: string
  ariaLabel: string
  external?: boolean
}

const { className, href, ariaLabel, external } = Astro.props
---

<a class={className} href={href} target={external ? '_blank' : '_self'} rel={external ? 'noopener noreferrer' : ''} aria-label={ariaLabel}>
  <slot />
</a>
